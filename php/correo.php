<?php

if (isset($_POST['enviar'])) {
  if (!empty($_POST['name']) && !empty($_POST['asunto']) && !empty($_POST[0msg]) && !empty['email']) {
    $name = $_POST['name'];
    $asunto = $_POST['asunto']
    $msg = $_POST['msg'];
    $email
  }
}
